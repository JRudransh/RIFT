# Generated by RIFT. Manual edits may be overwritten.
from dataclasses import dataclass
from typing import Literal

from decouple import config


@dataclass
class Config:
    DEV_MODE: bool = config("DEV_MODE", default=True, cast=bool)

    MONGO_URI: str = config("MONGO_URI")
    BASE_DB_NAME: str = config("BASE_DB_NAME", default="{{ package_name }}")

    JWT_SECRET: str = config("JWT_SECRET", default="secret_key")
    JWT_ALGORITHM: str = config("JWT_ALGORITHM", default="HS256")
    JWT_EXPIRATION: int = config("JWT_EXPIRATION", default=32400, cast=int)
    REFRESH_TOKEN_EXPIRATION: int = config("REFRESH_TOKEN_EXPIRATION", default=2592000, cast=int)

    AUTH_COOKIE_NAME: str = config("AUTH_COOKIE_NAME", default="{{ package_name }}_access_token")
    REFRESH_COOKIE_NAME: str = config("REFRESH_COOKIE_NAME", default="{{ package_name }}_refresh_token")
    COOKIE_SECURE: bool = config("COOKIE_SECURE", default=False, cast=bool)
    COOKIE_HTTPONLY: bool = config("COOKIE_HTTPONLY", default=True, cast=bool)
    COOKIE_SAMESITE: Literal["lax", "strict", "none"] = config("COOKIE_SAMESITE", default="lax")
    COOKIE_DOMAIN: str | None = config("COOKIE_DOMAIN", default=None)

    ADMIN_EMAIL: str = config("ADMIN_EMAIL")
    ADMIN_PASSWORD: str = config("ADMIN_PASSWORD")
    ADMIN_NAME: str = config("ADMIN_NAME")
